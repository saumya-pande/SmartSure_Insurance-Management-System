server:
  port: 8083

spring:
  application:
    name: claims-service
  datasource:
    url: jdbc:mysql://localhost:3306/claims_db?createDatabaseIfNotExist=true
    username: root
    password: 3141@MySQL
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

file:
  upload-dir: ./uploads/claim-documents/

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  swagger-ui:
    path: /swagger-ui.html
